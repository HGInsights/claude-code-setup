.DS_Store

# Local Claude Code overrides — machine-specific, not shared
.claude/settings.local.json

# Generated audit reports — these contain real repo names, file paths, session
# IDs and shell commands. This repo is public; never commit one. The tooling
# that produces them lives in its own repo (HGInsights/claude-code-audit), but
# a local run inside a clone of this repo must still never leave one behind.
CC-AUDIT.md
*-audit.json
cc-audit-reports/
audit/

__pycache__/
*.pyc
